name: Build and publish images and deploy to server

on:
  push:
    branches: [master]
    paths:
      - 'client/**'
      - 'server/**'
      - '.github/workflows/**'

permissions:
  contents: read
  packages: write

jobs:
  build-and-push-images:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v4
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Frontend image
        run: |
          cd client
          docker build -f Dockerfile.prod -t ghcr.io/leeviko/planit-client .
          docker push ghcr.io/leeviko/planit-client
      - name: Backend image
        run: |
          cd server
          docker build -f Dockerfile.prod -t ghcr.io/leeviko/planit-server .
          docker push ghcr.io/leeviko/planit-server
  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push-images
    steps:
      - name: Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          passphrase: ${{ secrets.SSH_PASSWORD }}
          script: |
            cd /srv/www/apps/planit
            bash deploy.sh
